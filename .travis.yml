sudo: required

language: ruby

services:
  - docker
  
before_script:
- docker login -u $DOCKER_USER -p $DOCKER_PASSWORD

script:
- docker build -t treezio/mugre-bot:latest .
- docker push treezio/mugre-bot:latest
